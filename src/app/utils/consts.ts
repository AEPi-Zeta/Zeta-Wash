const MACHINES_LIST = {
    'washer': {
        'key': 'washer',
        'count': 2,
        'default_minutes': 34,
    },
    'dryer': {
        'key': 'dryer',
        'count': 2,
        'default_minutes': 40,
    }
};

const DEFAULT_TAB = 0;

export default MACHINES_LIST;

// module.exports.MACHINES_LIST = MACHINES_LIST;
